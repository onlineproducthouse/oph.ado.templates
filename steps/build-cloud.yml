steps:
  - displayName: terraform init
    script: terraform -chdir=./module init

  - displayName: terraform validate
    script: terraform -chdir=./module validate

  - displayName: terraform plan
    script: terraform -chdir=./module plan -input=false -out=tfplan
