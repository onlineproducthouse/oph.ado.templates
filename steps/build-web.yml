parameters:
  - name: loadEnvVarScriptPath
    type: string

steps:
  - displayName: install nodejs
    task: NodeTool@0
    inputs:
      versionSource: spec
      versionSpec: 22.13.1

  - script: |
      source $(echo "${{ parameters.loadEnvVarScriptPath }}") \
        && npm run build
