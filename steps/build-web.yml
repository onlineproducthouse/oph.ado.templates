parameters:
  - name: dotEnvFilePath
    type: string

steps:
  - name: InstallNodeJS
    task: NodeTool@0
    inputs:
      versionSource: spec
      versionSpec: 22.13.1

  - name: BuildWeb
    script: |
      source $(echo "${{ parameters.dotEnvFilePath }}") \
        && npm run build
