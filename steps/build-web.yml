parameters:
  - name: dotEnvFilePath
    type: string

steps:
  - task: NodeTool@0
    inputs:
      versionSource: spec
      versionSpec: 22.13.1

    name: InstallNodeJS

  - script: |
      source $(echo "${{ parameters.dotEnvFilePath }}") \
        && npm run build

    name: BuildWeb
